{
    "name": "mern-project-console",
    "version": "2.0.0",
    "private": true,
    "description": "A modern full-stack MERN project console powered by Vite, React 19, Express 5, and MongoDB-ready APIs.",
    "workspaces": [
        "client",
        "server"
    ],
    "engines": {
        "node": ">=20.0.0",
        "npm": ">=10.0.0"
    },
    "scripts": {
        "dev": "concurrently -n server,client -c cyan,magenta \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
        "build": "npm run build --workspace client && npm run build --workspace server",
        "test": "npm run test --workspace server",
        "start": "npm run start --workspace server",
        "client": "npm run dev --workspace client",
        "server": "npm run dev --workspace server"
    },
    "devDependencies": {
        "concurrently": "^9.2.4"
    }
}
